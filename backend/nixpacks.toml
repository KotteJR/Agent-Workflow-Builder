[variables]
PYTHON_VERSION = "3.13"

[phases.setup]
nixPkgs = ["python313", "tesseract", "poppler_utils"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"

